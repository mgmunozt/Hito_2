<%= form_with(model: product) do |form| %>
  <% if product.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
        <% product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<div class="container-fluid">
  <section id="Producto" class="bg-dark text-white d-none d-sm-block">
    <div>
      <h3 class="text-center pt-5 pb-3">Productos</h3>
      <hr class="opacity-100" />
    </div>

    <form>
      <div class="form-group">
        <%= form.label :code, style: "display: block" %>
        <%= form.text_field :code, html_options = { class: "form-control" } %>
        </div>
      <div class="form-group">
        <%= form.label :description, style: "display: block" %>
        <%= form.text_field :description, html_options = { class: "form-control" }%>
      </div>
      <div class="form-group">
        <%= form.label :salesUnit, style: "display: block" %>
        <%= form.text_field :salesUnit, html_options = { class: "form-control" } %>
      </div>
      <%= form.submit %>

    <%# <button type="submit" class="btn btn-primary">Submit</button> %>
  </form>
  </section>
<% end %>
</div>



